Seem to be generating code.pot twice.

Make index.php choose language based on browser settings.

maitch pkg-config handling is broken: check for not_found should be outside
loop. Needs more sensible handling of exception in case pkg-config causes error.

maitch should be able to satisfy dependencies where the file depended on is
referred to with or without ${BUILD_DIR} or abs path in different places.
